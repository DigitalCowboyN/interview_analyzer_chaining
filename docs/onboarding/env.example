# ============================================
# Redis Configuration (Celery broker/backend)
# ============================================
# Redis is used for Celery task queue management
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND_URL=redis://redis:6379/1

# ============================================
# OpenAI API Configuration
# ============================================
# Get your key from: https://platform.openai.com/api-keys
# Required for sentence analysis via GPT-4o-mini
# Expected format: sk-proj-... or sk-...
# Cost: ~$2-5/month for development
# Recommended usage limit: $20/month
OPENAI_API_KEY=[YOUR_OPENAI_API_KEY]

# ============================================
# Google Gemini API Configuration
# ============================================
# Get your key from: https://makersuite.google.com/app/apikey
# Alternative LLM API (optional for some features)
# Expected format: AIzaSy...
GEMINI_API_KEY=[YOUR_GEMINI_API_KEY]

# ============================================
# Neo4j Database Configuration
# ============================================
# For local development, check the password in docker-compose.yml
# This MUST match the NEO4J_AUTH setting in docker-compose.yml
# Look for the line: NEO4J_AUTH: "neo4j/[PASSWORD]"
NEO4J_PASSWORD=[CHECK_DOCKER_COMPOSE_YML]

# ============================================
# EventStoreDB Configuration
# ============================================
# Connection string for event sourcing database
# Default for Docker Compose setup (container name: eventstore)
# Format: esdb://host:port?tls=false
ESDB_CONNECTION_STRING=esdb://eventstore:2113?tls=false

# ============================================
# Projection Service Configuration
# ============================================
# Enable the projection service that syncs EventStore to Neo4j
# Set to "true" to enable, "false" to disable
ENABLE_PROJECTION_SERVICE=true

# Number of parallel processing lanes for projections
# Recommended: 12 (increase for higher throughput, decrease for lower CPU usage)
PROJECTION_LANE_COUNT=12

# ============================================
# Optional: Override environment detection
# ============================================
# Uncomment to force environment type (usually auto-detected)
# The system auto-detects: docker (in container), ci (CI/CD), host (local Mac)
# ENVIRONMENT=host  # Options: host, docker, ci

# ============================================
# Optional: Azure Configuration (if using Azure services)
# ============================================
# Uncomment if you're using Azure-specific features
# AZURE_USERNAME=[YOUR_AZURE_USERNAME]
# AZURE_PASSWORD=[YOUR_AZURE_PASSWORD]

