# Docstring backlog

Modules with no module-level docstring — no derivable context. Burn this down: add a
docstring, run `make code-index`, and the module drops off this list.

**0 module(s)** remaining.
